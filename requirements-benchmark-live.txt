# Explicitly opt-in dependencies for ResearchQA live embedding and reranking.
# Install into the locked LocalRAG Python 3.11 environment; ordinary benchmark
# contract validation continues to use requirements-benchmark.txt only.
-r requirements-benchmark.txt
torch>=2.2,<3
transformers>=4.51,<5
accelerate>=1.0,<2
safetensors>=0.4,<1
huggingface-hub>=0.30,<1
sentencepiece>=0.2,<1
